// Servicio de Finanzas Avanzadas - SIMPLIFICADO TEMPORALMENTE
import { prisma } from '@/lib/db';

export async function calculateCashFlowForecast(companyId: string) {
  return [];
}

export async function detectFinancialAnomalies(companyId: string) {
  return [];
}

export async function generateBadDebtProvisions(companyId: string) {
  return [];
}
