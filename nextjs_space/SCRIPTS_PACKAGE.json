{
  "NOTA": "Agregar estos scripts al package.json existente",
  "scripts_to_add": {
    "postinstall": "prisma generate",
    "vercel-build": "prisma generate && prisma migrate deploy && next build",
    "migrate:prod": "prisma migrate deploy",
    "db:seed": "tsx --require dotenv/config scripts/seed.ts"
  },
  "instructions": [
    "1. Abrir /home/ubuntu/homming_vidaro/nextjs_space/package.json",
    "2. Agregar los scripts anteriores a la sección 'scripts'",
    "3. Guardar el archivo",
    "4. Los scripts permiten:",
    "   - postinstall: Genera automáticamente el cliente de Prisma",
    "   - vercel-build: Build personalizado para Vercel con migraciones",
    "   - migrate:prod: Ejecuta migraciones en producción",
    "   - db:seed: Pobla la base de datos con datos iniciales"
  ]
}
