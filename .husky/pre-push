echo "🚀 Running pre-push checks..."

# Run test suite (no watch, no coverage to keep it fast)
echo "🧪 Running all tests..."
npx vitest run || {
  echo "❌ Tests failed. Push aborted."
  exit 1
}

echo "✅ Pre-push checks passed!"
