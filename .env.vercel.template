# =====================================================
# INMOVA - Variables de Entorno para Vercel
# =====================================================
# Copia estas variables en Vercel Dashboard:
# Settings → Environment Variables
# =====================================================

# =====================================================
# 🔴 VARIABLES CRÍTICAS (OBLIGATORIAS)
# =====================================================

# Base de Datos PostgreSQL
DATABASE_URL='postgresql://USER:PASSWORD@HOST:PORT/DATABASE?connect_timeout=15&pool_timeout=15&connection_limit=10'

# NextAuth.js (Autenticación)
NEXTAUTH_SECRET='<EJECUTAR: openssl rand -base64 32>'
NEXTAUTH_URL='https://tu-proyecto.vercel.app'

# AWS S3 (Almacenamiento de archivos)
AWS_REGION='<REGION - ej: eu-west-1>'
AWS_BUCKET_NAME='<NOMBRE_DEL_BUCKET>'
AWS_FOLDER_PREFIX='<PREFIX - ej: inmova-production>'

# Stripe (Pagos)
STRIPE_SECRET_KEY='sk_live_XXXXXXXXXXXXXX'
STRIPE_PUBLISHABLE_KEY='pk_live_XXXXXXXXXXXXXX'
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY='pk_live_XXXXXXXXXXXXXX'

# Abacus AI (IA/LLM)
ABACUSAI_API_KEY='<TU_API_KEY>'

# Seguridad
ENCRYPTION_KEY='<EJECUTAR: openssl rand -hex 32>'
CRON_SECRET='<EJECUTAR: openssl rand -hex 32>'

# =====================================================
# 🟡 VARIABLES OPCIONALES (Recomendadas)
# =====================================================

# Sentry (Error Tracking)
SENTRY_DSN='<TU_SENTRY_DSN>'
NEXT_PUBLIC_SENTRY_DSN='<TU_SENTRY_DSN>'

# Redis/Upstash (Caching & Rate Limiting)
UPSTASH_REDIS_REST_URL='<TU_UPSTASH_URL>'
UPSTASH_REDIS_REST_TOKEN='<TU_UPSTASH_TOKEN>'

# Email (Nodemailer)
SMTP_HOST='smtp.gmail.com'
SMTP_PORT='587'
SMTP_USER='<TU_EMAIL>'
SMTP_PASSWORD='<TU_PASSWORD_APP>'
SMTP_FROM='INMOVA <noreply@inmova.app>'

# Web Push Notifications
NEXT_PUBLIC_VAPID_PUBLIC_KEY='<TU_VAPID_PUBLIC_KEY>'
VAPID_PRIVATE_KEY='<TU_VAPID_PRIVATE_KEY>'

# Stripe Webhook (configurar después del primer deploy)
STRIPE_WEBHOOK_SECRET='whsec_XXXXXXXXXXXXXX'

# =====================================================
# 🟢 VARIABLES OPCIONALES (Integraciones)
# =====================================================

# Twilio (SMS)
TWILIO_ACCOUNT_SID='<TU_ACCOUNT_SID>'
TWILIO_AUTH_TOKEN='<TU_AUTH_TOKEN>'
TWILIO_PHONE_NUMBER='<TU_NUMERO>'

# DocuSign
DOCUSIGN_ACCOUNT_ID='<TU_ACCOUNT_ID>'
DOCUSIGN_PRIVATE_KEY='<TU_PRIVATE_KEY>'
DOCUSIGN_BASE_PATH='https://www.docusign.net/restapi'

# Redsys / Open Banking
REDSYS_API_URL='https://apis.redsys.es/openapi/xs2a/api-entrada-xs2a/services'
REDSYS_OAUTH_URL='https://apis.redsys.es/openapi/xs2a/api-oauth-xs2a'
REDSYS_CLIENT_ID='<TU_CLIENT_ID>'
REDSYS_CLIENT_SECRET='<TU_CLIENT_SECRET>'

# =====================================================
# ⚙️ VARIABLES DE SISTEMA (Auto-configuradas por Vercel)
# =====================================================

NODE_ENV='production'
NEXT_PUBLIC_APP_URL='https://tu-proyecto.vercel.app'

# =====================================================
# 📝 NOTAS IMPORTANTES
# =====================================================
#
# 1. Nunca subas este archivo con valores reales a Git
# 2. Para generar secrets: ejecuta ./generate-secrets.sh
# 3. En Vercel, marca las variables como:
#    - Production: Para producción
#    - Preview: Para PRs/branches
#    - Development: Para desarrollo local (opcional)
# 4. Después de añadir variables, re-despliega con: vercel --prod
# 5. Para dominios custom, actualiza NEXTAUTH_URL y NEXT_PUBLIC_APP_URL
#
# =====================================================
