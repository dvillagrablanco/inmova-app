# INMOVA Production Environment Variables Template
# Copiar este archivo a .env.production y rellenar con valores reales

# Database
DATABASE_URL='postgresql://USER:PASSWORD@HOST:PORT/DATABASE?connect_timeout=15&pool_timeout=15&connection_limit=10'

# NextAuth
NEXTAUTH_SECRET='<GENERAR_CON: openssl rand -base64 32>'
NEXTAUTH_URL='https://www.inmova.app'

# AWS S3 (Cloud Storage)
AWS_PROFILE=hosted_storage
AWS_REGION=<REGION>
AWS_BUCKET_NAME=<BUCKET_NAME>
AWS_FOLDER_PREFIX=<FOLDER_PREFIX>

# Stripe (Producción)
STRIPE_SECRET_KEY='sk_live_<TU_CLAVE_SECRETA>'
STRIPE_PUBLISHABLE_KEY='pk_live_<TU_CLAVE_PUBLICA>'
STRIPE_WEBHOOK_SECRET='whsec_<TU_WEBHOOK_SECRET>'
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY='pk_live_<TU_CLAVE_PUBLICA>'

# Web Push Notifications
NEXT_PUBLIC_VAPID_PUBLIC_KEY='<TU_VAPID_PUBLIC_KEY>'
VAPID_PRIVATE_KEY='<TU_VAPID_PRIVATE_KEY>'

# Abacus AI (LLM APIs)
ABACUSAI_API_KEY='<TU_API_KEY>'

# Sentry (Error Tracking)
SENTRY_DSN='<TU_SENTRY_DSN>'
NEXT_PUBLIC_SENTRY_DSN='<TU_SENTRY_DSN>'

# Redis (Caching & Rate Limiting) - Opcional
REDIS_URL='redis://:<PASSWORD>@<HOST>:<PORT>'
UPSTASH_REDIS_REST_URL='<TU_UPSTASH_URL>'
UPSTASH_REDIS_REST_TOKEN='<TU_UPSTASH_TOKEN>'

# Email (Nodemailer)
SMTP_HOST='smtp.gmail.com'
SMTP_PORT=587
SMTP_USER='<EMAIL>'
SMTP_PASSWORD='<PASSWORD>'
SMTP_FROM='INMOVA <noreply@inmova.app>'

# Twilio (SMS) - Opcional
TWILIO_ACCOUNT_SID='<TU_ACCOUNT_SID>'
TWILIO_AUTH_TOKEN='<TU_AUTH_TOKEN>'
TWILIO_PHONE_NUMBER='<TU_NUMERO>'

# DocuSign - Opcional
DOCUSIGN_ACCOUNT_ID='<TU_ACCOUNT_ID>'
