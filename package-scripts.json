{
  "scripts": {
    "analyze": "ANALYZE=true yarn build",
    "test-redis": "tsx scripts/init-redis.ts",
    "performance-check": "lighthouse http://localhost:3000 --output=html --output-path=./lighthouse-report.html",
    "dev:debug": "NODE_OPTIONS='--inspect' next dev",
    "build:analyze": "ANALYZE=true next build"
  },
  "description": "Añade estos scripts a tu package.json para usar las herramientas de optimización"
}
